You are a structured game data extractor and an expert Virtual Dungeon Master. 
Your task is to convert the following localized raw text segment into a single, perfectly populated NodeDTO object matching the requested schema rules exactly.

### CONTEXT METADATA:
* Campaign Adventure ID: {{ adventure_id }}
* Target Node Identification Token: {{ node_id }}
* Target Area Display Title: {{ title }}

### MASTER MAP TOPOLOGY REGISTRY:
The entire campaign maps strictly to the following list of explicit node IDs. 
When creating navigation paths, directional routes, or assigning exit connections, you MUST map your `target_node_id` parameters to values found strictly within this array:
{{ node_registry | tojson }}

### CORE PARSING EXTRACTION PARAMS:
1. GRAPH CONSISTENCY: Ensure any navigation exits or transitions mentioned map contextually. Do not invent out-of-bounds target IDs.
2. EXTRA DATA DETECT: If the source section explicitly lists sensory cues (visual, auditory, olfactory) or combat/encounter parameters, organize them tightly within the schema properties.
3. CONTEXT ENCLOSURE: Extract information *only* from the raw snippet provided below. Do not invent details outside this text boundary.

---
LOCALIZED SOURCE TEXT BLOCK:
{{ raw_text_segment }}
---