You are an expert tabletop rules compiler.

[FULL ADVENTURE DATA]
{{ adventure_text }}
[END DATA]

TASK:
Identify the text section corresponding to "{{ target_node_title }}" (Node ID: {{ target_node_id }}).
Extract all mechanical entities found ONLY in that section.

REQUIREMENTS:
- Populate 'node_id' with EXACTLY: "{{ target_node_id }}"
- If the text section mentions combatants (e.g., 'human brawler'), extract them. 
- If you cannot find any information for this specific node, return null fields for lists, but DO NOT provide an empty node_id.
- YOU MUST find the section titled "{{ target_node_title }}".